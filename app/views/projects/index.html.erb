<h1>project index page</h1>

<%= link_to 'Sign out', destroy_user_session_path, method: :delete %><br><br>

<%= link_to "Create New project", new_project_path%><br><br>

<% @projects.each do |project| %>
 	<p>
	 	<%= link_to project do %><%= project.name %>
		<% end %>
	</p>
<% end %>