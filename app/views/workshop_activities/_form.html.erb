<%= form_with(model: [@workshop_activity.workshop.project, @workshop_activity]) do |form| %>

	<div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  

  <div class="actions">
    <%= form.submit %>
  </div>
  <% end %>